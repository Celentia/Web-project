Урок 1
Создать переменные всех типов(строки, числа, логический, undefined, null, объект)

Создать функцию в которой работает цикл do-while выход из которого контролирует confirm.
Конфирм спрашивает "хотите ли узнать тип любой из переменных"
Если ок - входим в тело цикла. вызываем prompt и спрашиваем имя переменной.
обрабатываем prompt. Если тип данных в данной переменной строка - алерт(это строка)
И так далее для всех типов данных.


Записать все переменные в массив.

Создать функцию, которая переберёт все элементы массива, 
и выведет элементы в таблицу из двух ячеек - первая - вивод самого элемента, 
вторая - результат работы оператора typeof().

Я сделал заготовку чтоб можно было просмотреть работу функций по нажатию на кнопку
Ваша задача передать в нужное место соответсвтующее имя функции
(комментарии в файле main.js помогут).

Всё на githubPages, чтоб я тоже понажимал и просмотрел работу функций. 